{
  "name": "dsa-battle-mono",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev:web":"pnpm --filter web dev",
    "build:ui":"pnpm --filter @dsa-battle/shared-ui build",
    "build:all":"pnpm run -r build",
    "build:parallel":"pnpm run --parallel -r build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
